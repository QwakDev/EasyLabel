import tkinter as tk
import os


def AppendLabelFile(): #TODO file of labels in .txt matching files in DIR  LABEL1,LABEL2,LABEL3...
    return 0
def GetNumberOfFiles(): #TODO count number of files in dir
    return 0
def GetListOfFiles(): #TODO RETURN List of files in dir
    return 0
def ReadFile(dir, file):#TODO read img and return it IN THE FUTURE transform
    return 0
def SetSavingDir():#TODO (*create dir AND LabelFile) OR (return number of files and labels) OR Output of wrong path given
    return 0
def SaveFile(img, label): #TODO save img to <x>.btm x=1,2,3... AND append label file with label
    return 0